# Build output (any configuration, e.g. build/, build-win/)
build*/
out/
.vs/
.vscode/
.cache/
CMakeUserPresets.json

# Ad-hoc captures from --screenshot / F2 (curated ones live in docs/)
*.ppm

# Game cache data (Jagex assets - never committed; drop your own OSRS cache
# here, e.g. from an OpenRS2 archive dump, to use --world)
cache/

STUDY.md
